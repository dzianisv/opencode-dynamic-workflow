# Reference clones (upstream source + prior art) — never committed
.references/

# Dependencies
node_modules/

# Build output (CI builds before publish; npm packs it via `files`)
dist/
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example

# OS / editor noise
.DS_Store
.claude/scheduled_tasks.lock

# opencode local working dir (plugin deps, persisted workflow scripts) — generated
.opencode/
